spring:
  application:
    name: msvc-shopping
  config:
    import: optional:configserver:http://localhost:8888
  cloud:
    config:
      uri: http://localhost:8888
      username: root
      password: pass4u
